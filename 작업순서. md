Phase 0 — 기반 정비 (MVP 영향 없음, 즉시 가능)
  ① components.js: DOC_TYPES 재정의 (rebuttal 추가, v2-planned, price 필드)
  ② doc_templates.py: 상대방 반박문 서식 가이드 추가
  ③ styles.css: 브랜드 컬러 #2D4DAA, 신규 CSS 토큰·클래스 추가

Phase 1 — 공통 컴포넌트 추출 (기존 기능 보존, 내부 리팩터)
  ④ Toast 컴포넌트 분리 (create_screen 인라인 → ui/Toast.js)
  ⑤ Modal 컴포넌트 신규 생성 (confirm / payment / guide)
  ⑥ SiteFooter 신규 생성
  ⑦ PaymentSummaryCard 신규 생성

Phase 2 — 기존 페이지 수정 (MVP 기능 보존하며 v1.2 반영)
  ⑧ home_screen.js: 공감 카피 + 6종 문서 + 건별 요금제
  ⑨ subscription_screen.js: 건별 결제 전면 교체 (Plan 컴포넌트 제거)
  ⑩ create_screen.js: 반박문 추가 + 위험 문장 검사 버튼
  ⑪ mydocs_screen.js: 탭 명칭 + DocChip 규칙

Phase 3 — 신규 화면·기능 추가 (확장)
  ⑫ dashboard_screen.js: DashboardScreen + prototype.js 라우트
  ⑬ EvidenceUploader (FR-17)
  ⑭ OpponentDocAnalysis + ClaimEvidenceRow (FR-18, 19)
  ⑮ 건별 결제 Modal 플로우 (FR-21)
  ⑯ server.py: 증거 파싱·상대방 분석·결제 API

Phase 4 — 품질 정리
  ⑰ PLAN_LIMITS / Plan 컴포넌트 / 구독 관련 코드 삭제
  ⑱ LegalNotice 전 화면 통일, SiteFooter 전 화면 적용
  ⑲ 전체 회귀 테스트 (기존 MVP 3종 문서 생성 흐름 확인)

kill 

fetch('/api/dev/reset', {method:'POST'}).then(r=>r.json()).then(console.log)